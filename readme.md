To generate clients and secrets for Passport:
`php artisan passport:install`

This will generate a Password grant client. Edit the client id and secret in login_controller.js to match the new values.
